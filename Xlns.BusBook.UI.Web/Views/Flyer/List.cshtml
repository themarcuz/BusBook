@model IEnumerable<Xlns.BusBook.Core.Model.Flyer>
@{
    ViewBag.Title ="Gestisci i tuoi Flyers";
}
@section head{
    <link href="@Url.Content("~/Content/tiledList.css")" rel="stylesheet" type="text/css" media="screen"/>
}
@section scripts{@*
    <script src="@Url.Content("~/Content/scripts/viaggio.lista.js")" type="text/javascript"></script>
*@}
<div id="content-wrapper" class="container_16">
    <div id="main" class="grid_6">
            <h3>@ViewBag.Title</h3>
@*        @using (Html.BeginForm("Save", "Flyer", FormMethod.Post, new { id = "flyerForm" }))
        {
            foreach (var item in Model)
            {
                Html.RenderAction("ViaggioTiledDetail", new { viaggio = item });
            }
        }*@
    </div>
</div>
